---
import Layout from '../layouts/Layout.astro';
import { ru as i18n } from '../i18n/ru';
---

<Layout title={`Партнёрам — ${i18n.brand.name}`}>
	<main>
		<section class="container section" id="partners">
			<div class="sectionHead">
				<h1 class="sectionTitle">{i18n.partners.title}</h1>
				<p class="muted sectionSubtitle">{i18n.partners.subtitle}</p>
			</div>

			<div class="featureGrid">
				{i18n.partners.items.map((v) => (
					<article class="featureCard card">
						<strong class="featureCard__title">{v.title}</strong>
						<div class="muted featureCard__text">{v.text}</div>
					</article>
				))}
			</div>

			<div style="margin-top: 18px;">
				<a class="btn btn-primary" href="/#contact">{i18n.partners.cta}</a>
			</div>
		</section>
	</main>
</Layout>

